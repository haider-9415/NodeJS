/*
    package.json file contains details of your project like version, git repositry
    commands, packages, etc.

    it is created by the command --> npm init
*/

const colors = require('colors')

console.log('this would be in red color'.red);
console.log('this would be in green color'.green);
console.log('this would be in blue color'.blue);


/*
    we will install 'nodemon' package it itself runs the file after changing and saving it
    
    to install it run --> npm i nodemon -g

    to run the file, run the code --> nodemon file_name
*/
console.log('this is NodeJS tutorial :)');

